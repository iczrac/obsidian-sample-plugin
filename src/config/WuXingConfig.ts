/**
 * 五行强度计算配置
 * 包含各种权重和计算参数
 */
export const WuXingConfig = {
  /**
   * 天干权重配置
   * 说明：天干权重表示天干在八字中的影响力，日主最重要，月令次之，年柱和时柱相对较轻
   */
  tianGanWeight: {
    year: 1.2,   // 年干权重（从1.0提高到1.2）
    month: 3.0,  // 月干权重（从2.5提高到3.0）
    day: 3.0,    // 日干权重（保持不变）
    hour: 1.0    // 时干权重（保持不变）
  },

  /**
   * 地支藏干权重配置
   * 说明：地支藏干权重表示地支藏干在八字中的影响力，月支最重要，日支次之
   */
  diZhiCangWeight: {
    year: 0.8,   // 年支藏干权重（从0.7提高到0.8）
    month: 2.5,  // 月支藏干权重（从2.0提高到2.5）
    day: 2.2,    // 日支藏干权重（从2.0提高到2.2）
    hour: 0.7    // 时支藏干权重（保持不变）
  },

  /**
   * 藏干内部权重配置
   * 说明：藏干内部权重表示多个藏干时的权重分配，主气最重要，中气次之，余气最轻
   */
  cangGanInnerWeight: {
    one: [1.0],                // 一个藏干：100%权重
    two: [0.6, 0.4],           // 两个藏干：60%和40%权重
    three: [0.5, 0.3, 0.2]     // 三个藏干：50%、30%和20%权重
  },

  /**
   * 纳音五行权重配置
   * 说明：纳音五行权重表示纳音在八字中的影响力，月柱最重要，日柱次之
   */
  naYinWeight: {
    year: 0.6,   // 年柱纳音权重（从0.5提高到0.6）
    month: 2.0,  // 月柱纳音权重（从1.5提高到2.0）
    day: 1.5,    // 日柱纳音权重（保持不变）
    hour: 0.5    // 时柱纳音权重（保持不变）
  },

  /**
   * 季节调整系数配置
   * 说明：季节调整系数表示五行在不同季节的旺衰程度
   */
  seasonAdjust: {
    wang: 2.5,   // 旺相系数（从2.0提高到2.5）
    xiang: 1.2,  // 相旺系数（从1.0提高到1.2）
    ping: 0.0,   // 平和系数（保持不变）
    qiu: -1.2,   // 囚系数（从-1.0增强到-1.2）
    si: -1.8     // 死系数（从-1.5增强到-1.8）
  },

  /**
   * 月令当令加成配置
   * 说明：月令当令加成表示当令五行和相旺五行的额外加成
   */
  monthDominantBonus: {
    dominant: 2.0,   // 当令加成（从1.5提高到2.0）
    related: 1.0,    // 相旺加成（从0.8提高到1.0）
    neutral: 0.0,    // 平和加成（保持不变）
    weak: -0.5,      // 囚加成（新增）
    dead: -0.8       // 死加成（新增）
  },

  /**
   * 组合关系权重配置
   * 说明：组合关系权重表示各种组合关系对五行强度的影响
   */
  combinationWeight: {
    tianGanWuHe: 0.8,      // 天干五合权重（从0.6提高到0.8）
    diZhiSanHe: 1.5,       // 地支三合权重（从1.2提高到1.5）
    diZhiSanHui: 1.2,      // 地支三会权重（从1.0提高到1.2）
    partialSanHe: 0.9,     // 部分三合权重（新增，为完整三合的60%）
    partialSanHui: 0.7     // 部分三会权重（新增，为完整三会的60%）
  },

  /**
   * 季节五行对应关系
   * 说明：各季节对应的五行旺衰状态
   */
  seasonWuXingStatus: {
    spring: {  // 春季
      mu: 'wang',    // 木旺
      huo: 'xiang',  // 火相
      tu: 'ping',    // 土平
      jin: 'qiu',    // 金囚
      shui: 'si'     // 水死
    },
    summer: {  // 夏季
      huo: 'wang',   // 火旺
      tu: 'xiang',   // 土相
      jin: 'ping',   // 金平
      shui: 'qiu',   // 水囚
      mu: 'si'       // 木死
    },
    autumn: {  // 秋季
      jin: 'wang',   // 金旺
      shui: 'xiang', // 水相
      mu: 'ping',    // 木平
      huo: 'qiu',    // 火囚
      tu: 'si'       // 土死
    },
    winter: {  // 冬季
      shui: 'wang',  // 水旺
      mu: 'xiang',   // 木相
      huo: 'ping',   // 火平
      tu: 'qiu',     // 土囚
      jin: 'si'      // 金死
    }
  },

  /**
   * 月令当令五行对应关系
   * 说明：各季节当令和相旺的五行
   */
  monthDominantWuXing: {
    spring: {  // 春季
      dominant: 'mu',    // 木当令
      related: 'huo'     // 火相旺
    },
    summer: {  // 夏季
      dominant: 'huo',   // 火当令
      related: 'tu'      // 土相旺
    },
    autumn: {  // 秋季
      dominant: 'jin',   // 金当令
      related: 'shui'    // 水相旺
    },
    winter: {  // 冬季
      dominant: 'shui',  // 水当令
      related: 'mu'      // 木相旺
    }
  }
};
